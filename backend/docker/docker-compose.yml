version: '3.8'
services:
  db:
    container_name: practice
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: f1-db
    ports:
      - "5500:5432" # port binding